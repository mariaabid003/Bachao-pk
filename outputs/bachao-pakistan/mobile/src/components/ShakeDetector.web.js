/**
 * ShakeDetector.web.js — Web stub for ShakeDetector
 *
 * Accelerometer (expo-sensors) is not available in browsers.
 * This no-op component is automatically picked by Metro over
 * ShakeDetector.js when bundling for the web platform.
 *
 * On web, SOS can be triggered via the "SOS" button instead of shaking.
 */
export default function ShakeDetector({ onShake }) {
  // No-op: no accelerometer API in browsers
  return null;
}
